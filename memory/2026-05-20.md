# 2026-05-20 日常记录

## AI 新闻日报 (12:27)
- 用户手动触发，代理已开启
- 采集来源: The Verge · AI News
- web_search (Gemini API) 在当前代理配置下连接失败，改用 web_fetch 替代

## 实验报告修改 (17:29 - 18:40)
- 用户基于 Gemini 生成的文案完成了电商商品数据集的报告
- 桌面 `1.docx` 需填充完整内容
- **v1-v5**: 多次尝试 python-docx 插入段落，因 `insert_paragraph_before`/XML `addprevious` 行为踩坑
- **v6**: 最终方案 — 用 `body.remove()` 清空章节间占位段落 + 从底向上用 XML `addprevious` 插入新内容
- **格式修正**: 字体修复(290处)、图片从描述段落分离(图5.2-5.4)、各图加居中加粗图注
- **目录**: 在封面后添加 TOC 域代码页，需用户手动右键更新域生成

## 关键教训
- `python-docx` 的 `insert_paragraph_before` 和 `addnext`/`addprevious` 在多段插入时需注意顺序
- `addprevious` 后插入的元素会出现在参考元素之前；多次调用时最后一个调用最靠近参考元素
- 处理 Word 图片时需在 `w:drawing` XML 层面操作
