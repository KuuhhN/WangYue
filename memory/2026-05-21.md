# 2026-05-21 每日日志

## 20:03 — 错误日志分析 + 系统优化

### 问题诊断
分析 `gateway.err.log` 发现 4 大异常：
1. **model-resolution + auth 退化**：从 05-15 修复后的 0ms/6ms 回到 46-61s
2. **Event loop 饥饿**：峰值 95s 延迟，CPU 100%
3. **Feishu 通道保活超时 30s**：open.feishu.cn/ping 10s 超时
4. **web_search 连续失败 (12:00 cron)**

### 修复执行
1. **🏗️ 归档超大 trajectory 文件** (5 个文件共约 24MB → 7.7MB)
   - `d52783e6...trajectory.jsonl` 10.2MB → sessions_old/
   - `0e56a9f2...trajectory.jsonl` 4.0MB → sessions_old/
   - 其他 3 个 >500KB 的旧文件 → sessions_old/
   - 归档 4 个 7 天前的 cron run 文件
2. **🔧 Config 优化**
   - `no_proxy` 添加 `api.deepseek.com` 显式域名
   - `web.search.timeoutSeconds: 15` 防止 web_search 无限超时
   - `handshakeTimeoutMs: 30000` WS 握手超时从 15s 提升到 30s

### 待验证
- Gateway 重新加载后 model-resolution 是否恢复 ms 级
- 当前会话无异常，可以正常回复

### 代理人
吴坤昊 - Clash 开启状态

---

## 21:51 — 系统验证（优化生效确认）

21:51 用户触发「执行」指令，执行系统状态验证：

**验证结果（全部通过 ✅）**
- **model-resolution**：秒级响应，无退化
- **Event Loop 延迟**：正常，无阻塞
- **飞书通道**：双向通，本消息送达成
- **trajectory 文件**：活跃 session 最大 414KB，10MB 怪物已归档
- **缓存命中率**：69% → 健康

**新增 cleanup-sessions.ps1**（22:09）
- 脚本路径: `scripts/cleanup-sessions.ps1`
- 清理规则: >500KB 或 >7 天 → 移入 sessions_old/
- cron 已挂: `sessions-auto-cleanup`，每周日 23:00 CST

## 22:37 — 用户主动触发封卷

今日总评：一次成功的性能事故响应 + 长效机制建立。从 20:03 发现问题到 22:37 封卷，2.5h 内完成了诊断、修复、验证、自动化四个阶段。
